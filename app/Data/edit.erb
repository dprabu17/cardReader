<div data-role="page">

  <div data-role="header" data-position="inline">
    <h1>Edit <%= @data.name %></h1>
    <a href="<%= url_for :action => :show, :id => @data.object %>" class="ui-btn-left" data-icon="back" data-direction="reverse">
      Cancel
    </a>
    <a href="<%= url_for :action => :delete, :id => @data.object %>" class="ui-btn-right" data-icon="delete" data-direction="reverse">
      Delete
    </a>
  </div>

  <div data-role="content">
    <form method="POST" action="<%= url_for :action => :update %>">
      <input type="hidden" name="id" value="<%= @data.object %>"/>
      
          <div data-role="fieldcontain">
            <label for="data[name]" class="fieldLabel">Name</label>
            <input type="text" id="data[name]" name="data[name]" value="<%= @data.name %>" <%= placeholder( "Name" ) %> />
          </div>
      
      <input type="submit" value="Update"/>
    </form>
  </div>

</div>
